# Connect Recordings

Store captured Infinite Flight Connect packets or normalized avionics snapshots here.

The replay path should become the default way to test display behavior without needing Infinite Flight running on the local network.

